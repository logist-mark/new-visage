import React from 'react';

const HeroSlider = () => {
    return (
        <div className="w-full h-[580px] bg-amber-500 bg-white/80 rounded-lg">
            HeroSlider
        </div>
    );
};

export default HeroSlider;